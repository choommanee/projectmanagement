{{- if .Values.notification.enabled -}}
{{- $svc := "notification-svc" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "platform.fullname" (dict "ctx" . "svc" $svc) }}
  labels:
    {{- include "platform.serviceLabels" (dict "ctx" . "svc" $svc) | nindent 4 }}
spec:
  replicas: {{ .Values.notification.replicas }}
  selector:
    matchLabels:
      {{- include "platform.selectorLabels" (dict "ctx" . "svc" $svc) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "platform.selectorLabels" (dict "ctx" . "svc" $svc) | nindent 8 }}
    spec:
      containers:
        - name: {{ $svc }}
          image: "{{ .Values.notification.image.repository }}:{{ .Values.notification.image.tag }}"
          imagePullPolicy: {{ .Values.notification.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.notification.service.targetPort }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.notification.service.targetPort | quote }}
            - name: DATABASE_URL
              value: {{ .Values.notification.env.DATABASE_URL | default .Values.postgres.dsn | quote }}
            - name: NATS_URL
              value: {{ .Values.notification.env.NATS_URL | default .Values.nats.url | quote }}
            - name: SMTP_HOST
              value: {{ .Values.notification.env.SMTP_HOST | quote }}
            - name: SMTP_PORT
              value: {{ .Values.notification.env.SMTP_PORT | quote }}
            - name: SMTP_USER
              value: {{ .Values.notification.env.SMTP_USER | quote }}
            - name: SMTP_PASSWORD
              value: {{ .Values.notification.env.SMTP_PASSWORD | quote }}
            - name: SMTP_FROM
              value: {{ .Values.notification.env.SMTP_FROM | quote }}
            - name: SMTP_TLS
              value: {{ .Values.notification.env.SMTP_TLS | quote }}
            - name: WEBHOOK_SIGNING_KEY
              value: {{ .Values.notification.env.WEBHOOK_SIGNING_KEY | quote }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.notification.env.OTEL_EXPORTER_OTLP_ENDPOINT | default .Values.observability.otlpEndpoint | quote }}
            - name: OTEL_SERVICE_NAME
              value: {{ .Values.notification.env.OTEL_SERVICE_NAME | quote }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 6
          resources:
            {{- toYaml .Values.notification.resources | nindent 12 }}
{{- end }}
